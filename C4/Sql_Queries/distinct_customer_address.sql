-- SQL SELECT DISTINCT REFERENCE
-- Purpose: Returns only unique values in a column, removing all duplicates.
-- Note: This is a DQL (Data Query Language) command.
-- Sandbox Status: Fully functional in Sandbox accounts.

SELECT
    DISTINCT customer_id
FROM
    `euphoric-glass-487215-c1.customer_data.customer_address`;
